# Execution Plan

Current priority:

1. Stabilize asset locking for product and model with fal Kling elements.
2. Improve Gemini shot plans so generated motion follows the reference structure.
3. Improve Product Selling Plan so product use and benefit proof are natural.
4. Keep UI simple and remove controls that do not change the current pipeline.
5. Show enough debug data to understand failures without exposing internal clutter.

Verification rules:

- Build/typecheck after code changes.
- Inspect local UI after frontend changes.
- Do not run paid video generation unless explicitly requested.

Next high-value improvements:

- Cleaner product image guidance and product crop/reference quality checks.
- Stronger shot-by-shot prompt compression for 5s, 10s, and 15s outputs.
- Optional advanced fields for benefits, banned phrases, and CTA.
